<div class="ios-navbar">
    <div class="container d-flex justify-content-between align-items-center h-100">
        <a class="navbar-brand ios-logo" href="">
            <span class="gold-gradient-text">AURUM</span> <span class="text-white fw-light">STUDIO</span>
        </a>
        
        <nav class="d-flex gap-4">
            <NavLink class="ios-link" href="" Match="NavLinkMatch.All">Головна</NavLink>
            <NavLink class="ios-link" href="masters">Майстри</NavLink>
            <NavLink class="ios-link" href="booking">Запис</NavLink>
        </nav>
    </div>
</div>

<style>
    .top-row {
    display: none !important; /* Повністю видаляємо цю білу смугу */
}

main {
    padding-top: 0 !important; /* Прибираємо відступ зверху */
}
    .ios-navbar {
        position: fixed;
        top: 0;
        left: 0;
        right: 0;
        height: 70px;
        z-index: 1000;
        background: rgba(20, 20, 20, 0.6);
        backdrop-filter: blur(20px) saturate(180%);
        -webkit-backdrop-filter: blur(20px) saturate(180%);
        border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    }

    .ios-logo {
        font-family: 'SF Pro Display', -apple-system, sans-serif;
        font-weight: 700;
        letter-spacing: -0.5px;
        text-decoration: none;
    }

    .gold-gradient-text {
        background: linear-gradient(135deg, #d4af37 0%, #f1c40f 100%);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
    }

    .ios-link {
        color: rgba(255, 255, 255, 0.7);
        text-decoration: none;
        font-weight: 500;
        font-size: 0.95rem;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        position: relative;
    }

    .ios-link:hover, .ios-link.active {
        color: #d4af37;
    }

    .ios-link.active::after {
        content: '';
        position: absolute;
        bottom: -8px;
        left: 0;
        width: 100%;
        height: 2px;
        background: #d4af37;
        border-radius: 10px;
        box-shadow: 0 0 10px rgba(212, 175, 55, 0.5);
    }
</style>